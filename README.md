# In‑Memory TSDB Project

## Overview
An in‑memory, sharded time‑series database in Python, with Prometheus monitoring, Grafana dashboards, Dockerized and Kubernetes‑ready.

## Quickstart
```bash
git clone <repo-url> tsdb_project
cd tsdb_project
docker-compose up --build
